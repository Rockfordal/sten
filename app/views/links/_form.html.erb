<%= simple_form_for(@link) do |f| %>
  <%= f.error_messages %>

    <%= f.input :title, :label => 'Titel' %>
    <%= f.input :url %>
    <%= f.association :linktype, :label => 'Länktyp' %>

  <%#= f.fields_for :properties do |builder| -%>
  <%# render 'property_fields', :f => builder %>
  <%# end -%>

  <p>
  <%= f.button :submit %>
<% end %>
<p>
