<%= simple_form_for(@user) do |f| %>
    <%= f.error_messages %>
    <%#= f.input :username %>
    <%#= f.input :email, :label => 'epost' %>
    <%= f.input :email %>
   <%= f.input :password %>
    <%#= f.input :password_confirmation %>
    <%= f.association :roles, :as => :select, :collection => Role.all, :label => 'Roller' %>
    <%#= f.input :person, :as => :select, :collection => Person.all %>
    <%#= f.submit 'Skapa' %>
    <%= f.submit %>
<% end %>
